# Dashboard container for Fly.io deployment
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir websockets

# Copy dashboard files
COPY dashboard/ .

# Expose ports
EXPOSE 8000 8765

# Run the dashboard server
CMD ["python3", "telemetry_receiver.py", "--mode", "simulate"]
